import nox.random

def churn(n: int) -> int:
    nox.random.seed(42)
    total: int = 0
    i: int = 0
    while i < n:
        total = total + nox.random.randint(0, 1000)
        i = i + 1
    return total

print(churn(1000000))
