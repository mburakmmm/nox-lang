import nox.time

def churn(n: int) -> int:
    total: int = 0
    i: int = 0
    while i < n:
        t: int = nox.time.now_ms()
        if t > 0:
            total = total + 1
        i = i + 1
    return total

print(churn(200000))
