def build_and_query(n: int) -> int:
    d: dict[int, int] = {0: 0}
    i: int = 1
    while i < n:
        d[i] = i * 2
        i = i + 1
    total: int = 0
    j: int = 0
    while j < n:
        total = total + d[j]
        j = j + 1
    return total

print(build_and_query(5000))
