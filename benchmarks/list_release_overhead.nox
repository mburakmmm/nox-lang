def make_list() -> list[int]:
    return [1, 2, 3, 4]

def run(n: int) -> int:
    total: int = 0
    i: int = 0
    while i < n:
        total = total + make_list()[0]
        i = i + 1
    return total

print(run(50000000))
