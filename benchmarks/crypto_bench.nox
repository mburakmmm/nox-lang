import nox.crypto

def churn(n: int) -> int:
    total: int = 0
    i: int = 0
    while i < n:
        h: str = nox.crypto.sha256("hello world " + str(i))
        total = total + len(h)
        i = i + 1
    return total

print(churn(10000))
