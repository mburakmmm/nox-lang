# Faz LL.1 (bkz. nox-teknik-spesifikasyon.md §3.71): repo'da bu dosya
# HİÇ YOKTU — GitHub Actions'ın windows-latest çalıştırıcıları
# `core.autocrlf=true` VARSAYILANIYLA geldiğinden, checkout SIRASINDA
# HER metin dosyası (özellikle `.nox`/`.zig`) SESSİZCE CRLF'ye
# çevriliyordu; Nox'un lexer'ı (`compiler/lexer/lexer.zig`) `\r`
# baytını HİÇ tanımadığından (yalnızca `\n` bekler), `stdlib/nox/
# core.nox` (HER programa otomatik birleştirilen dosya) DAHİL her
# `.nox` dosyası Windows'ta `UnexpectedCharacter` ile lex hatası
# veriyordu — GERÇEK bir `windows-latest` CI çalıştırmasında bulundu.
* text=auto eol=lf
