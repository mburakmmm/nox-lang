async def sum_list(xs: list[int]) -> int:
    total: int = 0
    i: int = 0
    while i < len(xs):
        total = total + xs[i]
        i = i + 1
    return total

xs: list[int] = [1, 2, 3]
t: Task[int] = spawn sum_list(xs)
print(await t)
