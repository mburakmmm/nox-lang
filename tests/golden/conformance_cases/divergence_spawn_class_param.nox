class Point:
    def __init__(self: Point, x: int, y: int) -> None:
        self.x = x
        self.y = y

async def sum_coords(p: Point) -> int:
    return p.x + p.y

pt: Point = Point(3, 4)
t: Task[int] = spawn sum_coords(pt)
print(await t)
