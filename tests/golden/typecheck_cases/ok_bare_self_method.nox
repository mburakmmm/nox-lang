class Counter:
    def __init__(self, start: int) -> None:
        self.value = start

    def bump(self) -> int:
        self.value = self.value + 1
        return self.value

c: Counter = Counter(0)
print(c.bump())
