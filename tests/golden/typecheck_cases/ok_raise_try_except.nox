class MyError:
    def __init__(self: MyError, code: int) -> None:
        self.code = code

def risky(x: int) -> int:
    if x < 0:
        raise MyError(1)
    return x

try:
    y: int = risky(-1)
except MyError as e:
    z: int = e.code
finally:
    pass
