class Point:
    x: int

    def __init__(self, x: int, y: int) -> None:
        self.x = x
        self.y = y

    def sum(self) -> int:
        return self.x + self.y

p: Point = Point(3, 4)
total: int = p.sum()
