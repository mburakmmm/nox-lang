import nox.thread

async def make_value(x: int) -> int:
    return x * 2

async def entry() -> None:
    t: Task[int] = spawn make_value(1)
    total: int = await t
    print(total)

nox.thread.pool_run(4, entry)
