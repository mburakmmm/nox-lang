# Faz FFI.4: `retains(does_not_exist)` VAR OLMAYAN bir parametre adı
# gösteriyor — checker'ın YENİ doğrulaması BUNU YAKALAMALI.
extern def nox_ffi4_take_list(xs: list[int]) -> None from "fake_ffi4.o" retains(does_not_exist)
