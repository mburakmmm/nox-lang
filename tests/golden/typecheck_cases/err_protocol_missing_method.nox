protocol Shape:
    def area(self: Shape) -> float:
        pass

class Point:
    def __init__(self: Point, x: int, y: int) -> None:
        self.x = x
        self.y = y

def print_area(shape: Shape) -> None:
    print(shape.area())

print_area(Point(1, 2))
