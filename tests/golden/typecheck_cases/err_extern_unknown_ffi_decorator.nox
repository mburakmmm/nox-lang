# v2.0 madde 2.2: `extern def`in decorator'ları (FuncDef'in aksine)
# derleyici tarafından GERÇEKTEN yorumlanır — tanınmayan bir isim
# sessizce yok sayılmaz.
@ffi.bogus("xs")
extern def nox_ffi22_unknown_decorator(xs: list[int]) -> None from "fake_ffi22.o"
