def read_it(xs: list[int]) -> int:
    return len(xs)

async def worker(xs: list[int]) -> None:
    n: int = read_it(xs)

async def entry() -> None:
    xs: list[int] = [1, 2, 3]
    t: Task[None] = spawn worker(xs)
    await t
