extern def counter_create(initial: int) -> ptr from "./build/counter.o"
extern def counter_get(handle: ptr) -> int from "./build/counter.o"

h: ptr = counter_create(10)
v: int = counter_get(h)
print(v)
