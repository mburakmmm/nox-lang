class Point:
    def __init__(self: Point, x: int, y: int) -> None:
        self.x = x
        self.y = y

def compute() -> int:
    total: int = 0
    lowlevel:
        p: Point = Point(3, 4)
        total = p.x + p.y
    return total
