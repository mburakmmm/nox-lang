extern def nox_ffi4_take_list(xs: list[int]) -> None from "fake_ffi4.o" retains(xs)

def compute() -> int:
    xs: list[int] = [3, 1, 4, 1, 5]
    nox_ffi4_take_list(xs)
    return len(xs)

print(compute())
