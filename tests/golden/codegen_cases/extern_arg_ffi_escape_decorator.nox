@ffi.escape("xs")
extern def nox_ffi4_take_list_decorator(xs: list[int]) -> None from "fake_ffi4_decorator.o"

def compute() -> int:
    xs: list[int] = [3, 1, 4, 1, 5]
    nox_ffi4_take_list_decorator(xs)
    return len(xs)

print(compute())
