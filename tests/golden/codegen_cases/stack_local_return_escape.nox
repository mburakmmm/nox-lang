class Point:
    def __init__(self: Point, x: int, y: int) -> None:
        self.x = x
        self.y = y

def make_point() -> Point:
    p: Point = Point(1, 2)
    return p

def compute() -> int:
    q: Point = make_point()
    return q.x + q.y

print(compute())
