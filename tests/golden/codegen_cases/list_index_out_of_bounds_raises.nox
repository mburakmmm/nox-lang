xs: list[int] = [10, 20, 30]

try:
    bad: int = xs[10]
    print(bad)
except IndexError as e:
    print(e.message)

try:
    bad2: int = xs[-1]
    print(bad2)
except IndexError as e2:
    print(e2.message)
