class Box:
    def __init__(self: Box, n: int) -> None:
        self.n = n

def read_box(b: Box) -> int:
    return b.n

def compute() -> int:
    b: Box = Box(7)
    return read_box(b)

print(compute())
