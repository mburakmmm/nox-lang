def compute(n: int) -> int:
    xs: list[int] = [1, 2, 3, 4, 5]
    total: int = 0
    i: int = 0
    while i < n:
        for v in xs:
            total = total + v
        i = i + 1
    return total

print(compute(500000))
