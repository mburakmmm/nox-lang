class Resource:
    def __init__(self: Resource, name: str) -> None:
        self.name = name

    def __enter__(self: Resource) -> None:
        print("enter")

    def __exit__(self: Resource) -> None:
        print("exit")

r: Resource = Resource("shared")
with r:
    print("body")
print("after")
