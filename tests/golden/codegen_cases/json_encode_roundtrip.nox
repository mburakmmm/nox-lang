import nox.json

s: str = "{\"a\":[1,2,true,null,\"hi\"],\"b\":3.5}"
v: JsonValue = nox.json.decode(s)
out: str = nox.json.encode(v)
print(out == s)
print(out)

v2: JsonValue = nox.json.decode(out)
print(nox.json.as_number(nox.json.object_value(v2, 1)))
