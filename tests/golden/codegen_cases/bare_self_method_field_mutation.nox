class Counter:
    def __init__(self, start: int) -> None:
        self.value = start

    def bump(self) -> int:
        self.value = self.value + 1
        return self.value

c: Counter = Counter(10)
print(c.bump())
print(c.bump())
print(c.value)
