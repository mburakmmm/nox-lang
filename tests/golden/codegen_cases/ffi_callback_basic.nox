def add_ints(a: int, b: int) -> int:
    return a + b

@ffi.callback("cb", "userdata")
extern def nox_test_invoke_callback(cb: (int, int, ptr) -> int, a: int, b: int, userdata: ptr) -> int from "fake_callback.o"

print(nox_test_invoke_callback(add_ints, 3, 4))
