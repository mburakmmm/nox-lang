class Bad:
    def __init__(self: Bad, xs: list[list[int]]) -> None:
        self.xs = xs

h: ptr = hpy_open("tests/compat/hpy_ext/noxtest.so", "noxtest")
b: Bad = Bad([[1, 2], [3, 4]])
print(hpy_call_on(h, "class_field_sum", b))
