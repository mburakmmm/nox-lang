def sum_it(xs: list[int]) -> int:
    total: int = 0
    for v in xs:
        total = total + v
    return total

def compute() -> int:
    ys: list[int] = []
    ys.append(1)
    ys.append(2)
    return sum_it(ys)

print(compute())
