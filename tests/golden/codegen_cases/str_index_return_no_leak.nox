def char_at(s: str, i: int) -> str:
    return s[i]

s: str = "loremipsum"
count: int = 0
i: int = 0
while i < 50000:
    c: str = char_at(s, i % len(s))
    if c == "m":
        count = count + 1
    i = i + 1
print(count)
