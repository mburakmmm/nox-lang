async def sub() -> int:
    return 1

async def worker() -> int:
    inner: Task[int] = spawn sub()
    x: int = await inner
    return x + 10

t: Task[int] = spawn worker()
y: int = await t
print(y)
