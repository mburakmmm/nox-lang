import nox.toml

text: str = "# top comment\n\nname = \"x\"  # trailing\n\n# another\n[section]\nval = 1\n\n"
root: nox.toml.TomlValue = nox.toml.parse(text)
print(root.tbl["name"].s)
print(root.tbl["section"].tbl["val"].i)
