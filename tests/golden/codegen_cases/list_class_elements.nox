class Point:
    def __init__(self: Point, x: int, y: int) -> None:
        self.x = x
        self.y = y

points: list[Point] = [Point(1, 2), Point(3, 4), Point(5, 6)]
for p in points:
    print(p.x)
    print(p.y)
