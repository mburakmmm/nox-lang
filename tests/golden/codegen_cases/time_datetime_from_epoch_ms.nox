import nox.time

dt: nox_time_DateTime = nox.time.from_epoch_ms(1700000000000)
print(dt.year)
print(dt.month)
print(dt.day)
print(dt.hour)
print(dt.minute)
print(dt.second)

now: nox_time_DateTime = nox.time.now()
print(now.year >= 2024)
