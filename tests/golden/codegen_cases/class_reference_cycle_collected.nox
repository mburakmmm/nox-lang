class Node:
    def __init__(self: Node, value: int) -> None:
        self.value = value
        self.next = self

a: Node = Node(1)
b: Node = Node(2)
a.next = b
b.next = a
print(a.value)
print(a.next.value)
print(b.next.value)
