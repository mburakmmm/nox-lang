import nox.gzip

data: list[int] = [0, 1, 2, 254, 255, 0, 255, 10, 20, 30]
compressed: list[int] = nox.gzip.compress_bytes(data)
back: list[int] = nox.gzip.decompress_bytes(compressed)
print(len(back))
print(back[0])
print(back[4])
print(back[9])
