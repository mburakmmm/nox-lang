extern def nox_ffi1_take_list(xs: list[int]) -> None from "fake_ffi1.o"

def compute() -> list[int]:
    xs: list[int] = [3, 1, 4, 1, 5]
    nox_ffi1_take_list(xs)
    return xs

print(len(compute()))
