def build() -> list[int]:
    xs: list[int] = []
    xs.append(1)
    xs.append(2)
    return xs

def compute() -> int:
    ys: list[int] = build()
    total: int = 0
    for v in ys:
        total = total + v
    return total

print(compute())
