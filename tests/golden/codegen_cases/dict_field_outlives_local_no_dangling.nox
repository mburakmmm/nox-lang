class Box:
    def __init__(self: Box, data: dict[str, str]) -> None:
        self.data = data

def make_box() -> Box:
    d: dict[str, str] = {"key": "value"}
    b: Box = Box(d)
    return b

box: Box = make_box()
print(box.data["key"])
