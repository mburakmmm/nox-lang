def apply(f: (int) -> int, x: int) -> int:
    return f(x)

def make_doubler() -> (int) -> int:
    def doubler(n: int) -> int:
        return n * 2
    return doubler

d: (int) -> int = make_doubler()
print(apply(d, 21))
