def make_pair() -> list[int]:
    return [10, 20]

def total(xs: list[int]) -> int:
    return xs[0] + xs[1]

print(total(make_pair()))

stored: list[int] = make_pair()
print(stored[0])
print(stored[1])
print(total(stored))
