import nox.strings

parts: list[str] = nox.strings.split("a,b,c", ",")
print(parts[0])
print(parts[1])
print(parts[2])

joined: str = nox.strings.join(parts, "-")
print(joined)

one: list[str] = nox.strings.split("hello", ";")
print(one[0])
