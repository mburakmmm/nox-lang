def run() -> int:
    s: str = "ab"
    i: int = 0
    ok: int = 0
    while i < 2:
        if i == 1:
            s = "abcdef"
        try:
            c: str = s[3]
            ok = ok + 1
        except IndexError:
            ok = ok + 100
        i = i + 1
    return ok

print(run())
