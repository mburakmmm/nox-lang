def g(n: int) -> int:
    total: int = 0
    i: int = 0
    while i < n:
        a: int = i % 3
        i = i * 2 + 1
        b: int = i % 3
        total = total + a * 10 + b
        i = i + 1
    return total

print(g(1))
print(g(2))
print(g(5))
