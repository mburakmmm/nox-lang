class Resource:
    def __init__(self: Resource, name: str) -> None:
        self.name = name

    def __enter__(self: Resource) -> str:
        print(self.name)
        return self.name

    def __exit__(self: Resource) -> None:
        print("exit")

with Resource("hello") as name:
    print(name)
print("after")
