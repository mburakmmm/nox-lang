class Box[T]:
    value: T
    def __init__(self: Box, value: T) -> None:
        if value == 0:
            raise ValueError("zero not allowed")
        self.value = value

def make_box(x: int) -> Box[int]:
    print("before")
    return Box[int](x)

make_box(0)
