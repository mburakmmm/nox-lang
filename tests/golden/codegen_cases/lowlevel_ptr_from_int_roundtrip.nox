def compute() -> int:
    total: int = 0
    lowlevel:
        p_as_int: int = 12345
        p: ptr = ptr_from_int(p_as_int)
        back: int = ptr_to_int(p)
        if back == p_as_int:
            total = 1
        else:
            total = 0
    return total

print(compute())
