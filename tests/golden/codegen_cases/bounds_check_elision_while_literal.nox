def sum_fixed() -> int:
    nums: list[int] = [1, 2, 3, 4, 5, 6, 7, 8]
    j: int = 0
    inner: int = 0
    while j < 8:
        inner = inner + nums[j]
        j = j + 1
    return inner

print(sum_fixed())
