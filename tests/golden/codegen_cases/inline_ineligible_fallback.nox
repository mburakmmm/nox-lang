def sum_range(n: int) -> int:
    total: int = 0
    i: int = 0
    while i < n:
        total = total + i
        i = i + 1
    return total

def fact(n: int) -> int:
    if n <= 1:
        return 1
    return n * fact(n - 1)

print(sum_range(5))
print(fact(5))
