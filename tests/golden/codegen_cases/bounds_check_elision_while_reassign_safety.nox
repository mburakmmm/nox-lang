def run(flag: bool) -> int:
    xs: list[int] = [1, 2, 3, 4, 5, 6, 7, 8]
    j: int = 0
    total: int = 0
    while j < 8:
        if flag:
            xs = [1, 2]
        total = total + xs[j]
        j = j + 1
    return total

print(run(False))
