import nox.toml
from nox.toml import TomlError

text: str = "s = \"unterminated\n"
try:
    root: nox.toml.TomlValue = nox.toml.parse(text)
    print("no-error")
except TomlError as e:
    print(e.message)
