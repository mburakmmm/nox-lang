def b_read(xs: list[int]) -> int:
    return len(xs)

def a_forward(xs: list[int]) -> int:
    return b_read(xs)

def compute() -> int:
    xs: list[int] = [1, 2, 3, 4]
    return a_forward(xs)

print(compute())
