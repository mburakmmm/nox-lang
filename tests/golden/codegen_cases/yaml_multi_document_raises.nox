import nox.yaml
from nox.yaml import YamlError

text: str = "a: 1\n---\nb: 2\n"
try:
    root: nox.yaml.YamlValue = nox.yaml.parse(text)
    print("no-error")
except YamlError as e:
    print(e.message)
