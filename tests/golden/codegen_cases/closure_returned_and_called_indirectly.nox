def make_adder(n: int) -> (int) -> int:
    def adder(x: int) -> int:
        return x + n
    return adder

add5: (int) -> int = make_adder(5)
add10: (int) -> int = make_adder(10)
print(add5(1))
print(add5(2))
print(add10(1))
