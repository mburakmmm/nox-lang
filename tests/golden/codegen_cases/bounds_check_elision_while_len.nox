def sum_while(xs: list[int]) -> int:
    total: int = 0
    j: int = 0
    while j < len(xs):
        total = total + xs[j]
        j = j + 1
    return total

xs: list[int] = [3, 1, 4, 1, 5, 9, 2, 6]
print(sum_while(xs))
